function principal(navigateTo) {
  const section = document.createElement('section');
  const prueba = document.createElement('h2');
  prueba.textContent = ('Hola');
  section.appendChild(prueba);
  return section;
}

export default principal;
