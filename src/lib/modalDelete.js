

/*const confirmationModal= document.createElement('div');
export function modalHTML(){
   confirmationModal.innerHTML='';
   const windowModal=`
<dialog id="delete-modal">
   <h2>Confirmar Eliminación</h2>
   <p>¿Estás seguro de que deseas eliminar esta receta?</p>
   <button id="confirm-delete">Eliminar</button>
   <button onclick="closeDeleteModal()">Cancelar</button>
</dialog>`;
confirmationModal.innerHTML+=windowModal
}

export function openDeleteModal(recipeContentid) {
    const modal = document.getElementById('delete-modal');
    const confirmDelete = document.getElementById('confirm-delete');
    confirmDelete.addEventListener('click', confirmDelete);
    confirmDelete.setAttribute (recipeContentid);
    
    modal.showModal();
}

export function closeDeleteModal() {
    const modal = document.getElementById('delete-modal');
    modal.close();
}

export function confirmDelete() {
    const modal = document.getElementById('delete-modal');
    const recipeId = confirmButton.getAttribute('data-recipe-id');

    modal.close();
}*/

  // Ventana modal
  //export function modalDeleteRecipe(recipeContentid) {
   


/*    modalContent.addEventListener('click',(e) => {
        const press = (e.target.id);
        if(press.includes('deleteButton')){
            deletePost();
        }
    });*/
    



    /*// Evento para ejecutar la función de borrar al dar click
    deleteButton.addEventListener('click', async () => {
      await deletePost(recipe.id);
      closeModal();
    });
    // Evento para cerrar la ventana modal al hacer clic en el botón "Cancelar"
    cancelButton.addEventListener('click', () => {
      closeModal();
    });
    return modal;*/
  
  
